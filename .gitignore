# Database (WAL/SHM temp files only)
breakout_tracker.db-wal
breakout_tracker.db-shm

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Environment
.env
.env.save

# Preview files
email_preview.html

# Backtest outputs
backtest_results/
portfolio_equity_curve.csv
portfolio_trade_log.csv

# Live trading state — tracked in repo so GitHub Actions can persist updates

# OS
.DS_Store

# Conversation/context handoffs from prior Claude threads — kept locally
# for reference but not part of repo history (HANDOFF.md is canonical).
.handoffs/
CONTEXT_HANDOFF*.md
